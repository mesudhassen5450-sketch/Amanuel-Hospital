const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const hash = await bcrypt.hash('admin123', 10); await prisma.staffAccount.upsert({ where: { username: 'admin' }, update: { password: hash, role: 'ADMIN' }, create: { username: 'admin', password: hash, name: 'System Admin', role: 'ADMIN', department: 'Administration' } }); console.log('? Admin credentials guaranteed: username=admin | password=admin123'); await prisma.$disconnect(); } main();
